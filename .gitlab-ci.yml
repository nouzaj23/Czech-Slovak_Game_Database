default:
  image: docker:latest
  tags:
    - shell

build-job:
  stage: build
  script:
    - docker compose build
    - echo ${DOTNEV} > .env
  only:
    - main

test-job1:
  stage: test
  script:
    - echo "This job tests something"
  only:
    - main

deploy-job:
  stage: deploy
  script:
    - docker compose up -d --force-recreate
    - echo ${DOTNEV} > .env
  only:
    - main
